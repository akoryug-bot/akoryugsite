import { Button } from "@/components/ui/button"

// Simple toaster mock since it's just ui/toaster we need to import to avoid crash
import * as React from "react"
export function Toaster() {
  return null;
}
